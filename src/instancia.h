#pragma once
#include <string>
#include "grafo.h" // Inclui grafo.h para usar a struct Grafo

using namespace std;

// Declaração da função para processar o arquivo de instância
void processarInstancia(const string &caminhoArquivo, Grafo &g);